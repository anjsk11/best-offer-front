# Local dev uses the Vite /api proxy when this is empty.
VITE_API_BASE_URL=

# Use this for a static deployment that is not served behind the same API proxy.
# VITE_API_BASE_URL=http://43.201.197.227:8080
